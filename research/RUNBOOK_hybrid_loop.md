# USER RUNBOOK — kickoff prompt for the hybrid-experiment session

**Who runs this:** Farron (the user), not an agent.
**When:** in the DEDICATED executor session for the FRONTIER 19/20 hybrid
veil-recovery experiment (fresh session, or the one already underway), whenever
leaving it to work unattended. Paste as one message:

```
/loop 20m continue the FRONTIER 19/20 hybrid work until completely finished — on
first firing, if you haven't already: read MISSION → DEVSTYLE → PLAYBOOK →
SYNTHESIS → FRONTIER 19/20 and work under them (DOCTRINE line on reports).
Commit and push at every checkpoint so nothing is lost; log a FINDINGS entry and
update FRONTIER status per milestone. When implementation is done, run the
dataset evaluations and commit the main evidence: results, data, and which
images were used/trained on. If you're mid-phase when this fires, just continue
it. Be creative and research-driven, not legalistic — but disciplined and
grounded in the docs, adding to the docs as fit (negatives are findings, not
failures). I am not present: don't ask for input, don't get stuck — decide per
the docs and keep going.
```

Notes: 20m interval is a fallback heartbeat (background-job completions re-wake
the session on their own; 5m would interrupt long factory sweeps). The manager
session (camera-master1) stays hands-off the executor's research files while the
loop runs, and performs the critical integration read when evidence lands.
